/**
 * No released version has needed a config or action migration yet. New scripts
 * append here; they must never be reordered or removed, because Companion
 * records how many have run rather than which.
 */
export const UpgradeScripts = [];
